services:
  - type: web
    name: duty-journal
    runtime: node
    buildCommand: cd server && npm install
    startCommand: cd server && node server.js
    envVars:
      - key: JWT_SECRET
        generateValue: true
      - key: PORT
        value: 3000